[idx-local-file-in]
TRANSFORMS-routing = send-to-idx-local-file-in

[idx-local-tcp-in]
TRANSFORMS-routing = send-to-idx-local-tcp-in

[idx-depl-file-in]
TRANSFORMS-routing = send-to-idx-depl-file-in

[idx-depl-tcp-in]
TRANSFORMS-routing = send-to-idx-depl-tcp-in
