[idx-local-file-in]
TRANSFORMS-routing = send-to-idx-local-file-in

[idx-local-tcp-in]
TRANSFORMS-routing = send-to-idx-local-tcp-in

[idx-clst-file-in]
TRANSFORMS-routing = send-to-idx-clst-file-in

[idx-clst-tcp-in]
TRANSFORMS-routing = send-to-idx-clst-tcp-in
